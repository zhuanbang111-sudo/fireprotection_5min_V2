# 消防设施可达性评估系统 — 用户操作手册 (User Manual)

本手册旨在指导城市规划人员、消防指挥官以及 GIS 专业技术人员，如何使用**消防设施可达性评估系统**开展高精度的“5分钟消防等时圈”可达性仿真推演与规划决策。

---

## 目录
1. [系统访问与账户授权](#1-系统访问与账户授权)
2. [高德地图 API Key 配置（多秘钥轮换）](#2-高德地图-api-key-配置多秘钥轮换)
3. [待评估数据准备（Excel/CSV 字段标准）](#3-待评估数据准备excelcsv-字段标准)
4. [核心规划与仿真参数配置](#4-核心规划与仿真参数配置)
5. [模型智能标定（基于实测数据驱动）](#5-模型智能标定基于实测数据驱动)
6. [开始分析与地图交互可视化](#6-开始分析与地图交互可视化)
7. [分析成果持久化备份与并网对比](#7-分析成果持久化备份与并网对比)
8. [多格式成果导出（Excel / GIS 专业 Shapefile）](#8-多格式成果导出excel--gis-专业-shapefile)
9. [常见问题与排障指南 (FAQ)](#9-常见问题与排障指南-faq)

---

## 1. 系统访问与账户授权
系统的底层数据（历史成果、系统参数）受数据库保护，首次使用必须进行身份认证：
1. **注册与登录**：
   - 访问应用地址，系统会自动弹出安全登录窗口。
   - 输入您的电子邮箱与密码。若无账号，点击“立即注册电子邮箱独立账号”一键创建。
2. **试用与升级 (PRO)**：
   - 新注册用户默认为 **【免费试用】** 状态。
   - 免费试用账户每日限制分析站点数量、API 消耗上限。同时，ArcGIS/QGIS 兼容的 **Shapefile 导出** 为 **PRO 专业版** 专业功能。
   - 要升级至 PRO 专业版，请在顶部导航栏点击 **"Upgrade PRO"** 徽章，在弹出的“特权申请”面板中升级，以解除并发上限与矢量包限制。

---

## 2. 高德地图 API Key 配置（多秘钥轮换）
评估系统使用高德地图 Web 服务引擎进行真实路网拓扑分析与距离、路况探测。
1. **秘钥申请**：
   - 注册并登录 [高德开放平台](https://lbs.amap.com/)。
   - 创建一个“Web 服务”类型的应用，并申请对应的 **API Key**。
2. **多秘钥负载均衡（核心特护机制）**：
   - 如果您要进行几十个甚至上百个站点的大规模批处理，单个 Key 可能会由于并发或单日额度超限遭到高德引擎阻断。
   - **操作方法**：在侧边栏的 **“高德地图 API Key”** 输入框中，填入多个 API Key，**使用半角英文逗号 `,` 分隔**（例如：`key1,key2,key3`）。
   - **工作原理**：仿真后端在发起路网查询时，会自动检测秘钥可用性，并在多秘钥之间实现轮换与自动负载均衡。
   - **隐私保护**：本系统实施严苛的安全协议，用户在前端填写的秘钥仅保存在当前浏览器的本地缓存（LocalStorage）或内存中作传输代理，**不会**在云端服务器进行明文落地持久化。

---

## 3. 待评估数据准备（Excel/CSV 字段标准）
工具支持一键导入待测量的消防站、微型消防站或待分析的规划站点：
1. **坐标系支持**：
   - 默认支持 **GCJ-02 (高德火星坐标系)** 坐标，该格式与高德路网计算天然对齐。
   - 同时也原生兼容 **WGS-84 (GPS/无偏移地球坐标系)** 与 **BD-09 (百度坐标系)**。
2. **表格格式规范**：
   - 准备一个 `*.xlsx`, `*.xls` 或 `*.csv` 电子表格。
   - 必须包含以下三列，且首行必须为字段头：
     - `station_name`：站点（消防站）名称
     - `lng`：经度（如 `116.3974`）
     - `lat`：纬度（如 `39.9087`）
3. **导入数据**：
   - 点击侧边栏的 **“选择 Excel/CSV 待评估数据”**，或直接将文件拖拽至虚线区域内。
   - 导入后，侧边栏会实时解析并列表展示所有的待评估站点。此时可以手动选择部分或全部站点参与本次计算。

---

## 4. 核心规划与仿真参数配置
区别于传统的直线缓冲区（Buffer）或基础导航规划，本引擎深度融合了特种出警物理限制和消防车应急特权算法：
1. **目标到达时间**：
   - 默认设为 **5 分钟**（即城市消防“5分钟等时圈”黄金出警时间标准）。
2. **坐标系映射**：
   - 在选择框中指定您上传数据的 **原始坐标系**（GCJ-02 / WGS-84 / BD-09），系统会自动执行底层纠偏，确保坐标在空间底图和高德路网上严格一致。
3. **消防特权系数 (Factor)**：
   - 该系数调谐社会交通流对消防车的迟滞约束度。取值在 `0.1` 至 `1.0` 之间，**数值越小代表消防车的通行特权等级越高（速度补偿越大）**。
   - *默认推荐值 `0.8`*。该值通过后台算法优化了“掉头动作耗时（压缩至正常导航的10%）”和“逆行翻越护栏路径（70%提速）”。
4. **末端地块行驶速度 (Plat Speed)**：
   - “最后100米”纠偏参数。高德导航只能引导车辆到达路边，而从大门到起火位置的行驶过程。
   - 允许设定 `0 - 15 m/s` 的地块内穿行速度，模型将自动累加距离并代入最终总耗时。
5. **步行速度补偿**：
   - 专用于极冷门区域、末端死胡同网格等时圈的边缘插值补偿（默认 `4.0 km/h`）。

---

## 5. 模型智能标定（基于实测数据驱动）
如果您期望根据某一特定城市的**实际历史消防出警记录**来获得最逼真、最科学的仿真系数，请使用**“自动标定”**功能。
1. **标定文件准备**：
   - 收集城市以往的历史出警记录，整理成一张电子表格。
   - 文件须包含：
     - `lng` 与 `lat`：起火点/出警终点的坐标
     - `actual_time`：**实测出警总耗时（单位：分钟）**
2. **执行标定**：
   - 切换到界面中的 **“模型自动标定”** 功能卡片。
   - 上传上述历史出警记录，选择标定坐标系。
   - 点击 **“执行算法标定”**。后端标定器会通过穷举和最小二乘法网格搜索，在数百组参数中循环拟合，推荐出最符合本地区真实交通状况的 **特权系数** 与 **地块速度**。
3. **参数应用**：
   - 标定成功后，点击 **“直接应用标定参数”**，这些系统寻优参数会瞬间覆盖并应用到您的可达性推演主面板。

---

## 6. 开始分析与地图交互可视化
1. **启动计算**：
   - 参数配置无误后，点击底部的 **“开始分析可达性”**。
   - 系统将动态向后端发起批量锚点探测，并在下方的 **“运行系统日志”** 框中输出实时的计算进程、纠偏状态与报错情况。
2. **多源地图图底切换**：
   - 默认提供高品质的 **“天地图矢量底图”**，在政务展示与大国测绘规范中对齐标准。
   - 此外，可点击地图右上角图例，自由切换至 **“天地图卫星影像底图”**（查看建筑物地貌细节）或 **“OpenStreetMap 便携路网图”**。
3. **等时圈渲染与交互**：
   - 计算完成后，地图会实时增量渲染每个站点的 “5分钟等时圈多边形”。
   - **交互**：鼠标移动到多边形上，会显示高亮边缘。点击任意等时圈，会弹出“空间成果属性框”，显示该站点等时圈的实际可达覆盖面积（平方公里）、分析耗时等信息。

---

## 7. 分析成果持久化备份与并网对比
本系统提供生产级的安全历史备份架构，不仅能把数据保留在本地，还能同步到企业的高安全关系数据库中。
1. **上传保存当前成果**：
   - 在主界面下方 “分析成果统计” 面板右侧，有一个 **“备份当前大盘成果”** card。
   - 输入一个便于识别的任务名称（如 `北京天安门中队2026版规划成果`）。
   - 点击 **“同步保存当前运行结果”**，大盘包含的所有站点等时圈几何拓扑多边形、分析细节和参数都将同步打包，一键持久化保存在云端。
2. **历史记录调用与回载**：
   - 点击侧边栏的 **“历史备份目录”**，即可查阅您名下所有的备份报告。
   - 选择一条历史记录，点击 **“📂 回载成果”**，地图和报盘将立即返回至保存时的状态，方便随时向上级汇报。
3. **并网多区域对比分析**：
   - 假设您手头已有当前分析结果，并想和上个月的对比。
   - 在历史备份目录中，点击该历史记录右侧的 **“🔗 增量并网对比”**。
   - 系统会把历史等时圈加入目前的可视化大盘中作为比对图层（自动为历史记录重命名为 `[历史分析数据] 消防站名` 以防冲突），极高提升了空间重叠率、拓扑盲区的直观比对效率。

---

## 8. 多格式成果导出（Excel / GIS 专业 Shapefile）
模型分析产生的空间要素，为您提供了双媒介形式的高精资产文件：
1. **Excel 审计报表导出**：
   - 点击 **“导出 Excel”**。
   - 系统会即刻将出警点列表、实际覆盖面积（$km^2$）、周边探测检索出的各项公共设施明细（POI 兴趣点归类统计，如医院分级、重点保障人口密集区、特殊重点 POI 计数）等表格属性以高度规整的结构输出为 Excel 资产。
2. **GIS 行业绝对中介：Shapefile 导出 (WGS-84)**：
   - *（该功能为 PRO 专业版用户专属）*。
   - 点击 **“导出 SHP (WGS84)”**。
   - 系统将在边缘端一秒打包生具有精密拓扑坐标、符合 WGS-84 世界地球标准椭球体（零漂移无间隙）的分层面要素 Shapefile 资源压缩包（包含 `.shp`, `.dbf`, `.shx`, `.prj` 等全套文件）。
   - 下载包后，可**无缝无损地拖拽进 ArcGIS、QGIS、超图 SuperMap** 进行二次深度可达性空间分析或学术海报排版出图。

---

## 9. 常见问题与排障指南 (FAQ)
* **Q1：为什么地图上没有显示等时圈，甚至日志框提示 API 报错？**
  * **答**：请优先检查您填写的“高德 API Key”是否正确、是否勾选或启用了“Web 服务”API 类权限。如果使用免费限额 Key，可以填入多个 Key 用英文逗号进行逗号轮换。
* **Q2：导入的站点在地图上的位置怎么和底标有几百米到一公里的整体偏移？**
  * **答**：这是坐标系不统一导致的漂移。请在左侧侧边栏确认“上传原始坐标系”是否与您表格中的经纬度格式一致，如您的数据是从 GPS（WGS-84）导出的，应该将设置改为 `WGS-84`；如果是在高德网页拾取的，应设为 `GCJ-02`。
* **Q3：为什么点“下载 Shapefile”系统弹窗被拦截，或者提示不是 PRO 会员？**
  * **答**：专业矢量 Shapefile 打包是本系统的进阶专享功能。为保护边缘端计算性能以及遵守系统资产规则，免费注册账号不提供导出。请点击头像旁的 VIP 状态区域，在弹窗中点“申请升级 PRO 专业版”升级以解锁全部功能。
* **Q4：系统中的备份能保存多少时间？可以由他人调阅吗？**
  * **答**：在安全登录架构下，您创建的所有快照全部与您的电子邮箱独立账号执行严格的多租户强隔离存储，其他用户无法调阅，保障规划项目的绝对涉密安全。备份会永久保留在数据库中，除非您手动执行删除。
